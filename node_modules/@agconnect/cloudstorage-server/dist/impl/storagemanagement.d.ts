import { Bucket } from './bucket';
import { StorageOptions } from '../model/options';
import { CreateBucketResponse, GetBucketsResponse } from '../model/callback';
import { BucketMetadata } from '../model/models';
export declare class StorageManagement {
    constructor(options?: StorageOptions);
    bucket(name: string): Bucket;
    createBucket(name: string, metadata?: BucketMetadata): Promise<CreateBucketResponse>;
    getBuckets(): Promise<GetBucketsResponse>;
    private randomString;
}
