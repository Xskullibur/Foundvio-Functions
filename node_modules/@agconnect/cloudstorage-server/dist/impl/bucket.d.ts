import { StorageManagement } from './storagemanagement';
import { BucketExistResponse, CreateBucketResponse, GetFilesResponse, UploadResponse } from '../model/callback';
import { DeleteFileOptions, GetFilesOptions, UploadOptions } from '../model/options';
import { File } from './file';
import { BucketMetadata, Cors } from '../model/models';
export declare class Bucket {
    /**
     * bucket name
     */
    name: string;
    /**
     * storage instance
     */
    storage: StorageManagement;
    constructor(storage: StorageManagement, name: string);
    create(metadata?: BucketMetadata): Promise<CreateBucketResponse>;
    delete(): Promise<void>;
    deleteFiles(options?: DeleteFileOptions): Promise<void>;
    exists(): Promise<BucketExistResponse>;
    file(name: string): File;
    getFiles(options?: GetFilesOptions): Promise<GetFilesResponse>;
    setCorsConfiguration(config: Array<Cors>): Promise<void>;
    setMetadata(metadata: BucketMetadata): Promise<void>;
    upload(pathStr: string, options?: UploadOptions): Promise<UploadResponse>;
}
