"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccessToken = void 0;
var AccessToken = /** @class */ (function () {
    function AccessToken(token, expirationTime) {
        this.token = token;
        // 过期时候的毫秒数时间
        this.expirationTime = expirationTime;
    }
    AccessToken.prototype.getToken = function () {
        return this.token;
    };
    AccessToken.prototype.isValid = function () {
        var currentTime = new Date().getTime();
        var validTime = this.expirationTime - AccessToken.TWO_MINUTES_EARLY;
        return null != this.token && currentTime <= validTime;
    };
    AccessToken.TWO_MINUTES_EARLY = 2 * 60 * 1000;
    return AccessToken;
}());
exports.AccessToken = AccessToken;
